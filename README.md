# laravel-redis-throttle
Built a complete, working Laravel package: laravel-redis-throttle — a Redis-backed rate limiter with per-user/per-route buckets, custom X-RateLimit-* headers, and a programmatic reset API.
